# Destinations

A `Destination` represents a region travellers explore. It has an image,
description, location, a list of things to do, associated `Place`s, related
`TourPackage`s, and a map. Destinations and places are reused across
multiple packages rather than duplicated per package.
