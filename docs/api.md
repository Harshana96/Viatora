# API

The MVP favors Server Actions over a REST API. Route handlers under
`src/app/api/` are only used where a plain HTTP endpoint is required (e.g.
webhooks). Document any such endpoint here as it's added.
